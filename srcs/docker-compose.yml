version: '3'

services:
  nginx:
    build:
      context: .
      dockerfile: requirements/nginx/Dockerfile
    container_name: nginx
#     depends_on: # nginx는 빠르게 빌드되기 때문에 충돌을 피하기 위해서 wordpress container가 빌드될 때까지 기다리고 빌드를 시작하게 만든다.
#       - wordpress
    ports:
      - "443:443"
    volumes:
      #      - ./requirements/nginx/conf/:/etc/nginx/http.d/
      #      - ./requirements/nginx/tools:/etc/nginx/ssl/
      - /home/yichoi/simple_docker_nginx_html/public/html:/var/www/
    restart: always
